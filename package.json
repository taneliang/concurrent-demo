{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "prestart": "cp -ur ../react/build/node_modules/* ./node_modules/",
    "prebuild": "cp -ur ../react/build/node_modules/* ./node_modules/",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
